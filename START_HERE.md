# 🚀 Быстрый старт AI Video Generator Bot

## Проект запущен и готов к работе!

### Текущий статус:
✅ Docker контейнеры собраны и запущены
✅ Все сервисы работают (Redis, Web, Worker)
✅ FFmpeg установлен и настроен
✅ API endpoints доступны

---

## Управление проектом

### Запуск
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Проверка статуса
```bash
docker-compose ps
```

---

## Тестирование

### Проверка работы API
```bash
curl http://localhost:5000/health
```

Ожидаемый ответ:
```json
{"service":"ai-video-generator-bot","status":"healthy"}
```

---

## Настройка Telegram Webhook (для тестирования бота)

### Вариант 1: Использование ngrok (рекомендуется для локального теста)

1. Установите ngrok: https://ngrok.com/download

2. Запустите туннель:
```bash
ngrok http 5000
```

3. Скопируйте HTTPS URL (например: https://abc123.ngrok.io)

4. Обновите .env:
```
TELEGRAM_WEBHOOK_URL=https://abc123.ngrok.io
```

5. Перезапустите контейнеры:
```bash
docker-compose restart
```

### Вариант 2: Деплой на Render.com

1. Создайте аккаунт на https://render.com
2. Подключите GitHub репозиторий
3. Создайте Web Service из docker-compose.yml
4. Добавьте переменные окружения из .env
5. Деплой произойдет автоматически

---

## Структура проекта

```
video4min/
├── app/                    # Основной код приложения
│   ├── bot/               # Telegram bot handlers
│   ├── models/            # Data models
│   ├── services/          # Business logic
│   ├── tasks/             # Celery tasks
│   └── utils/             # Utilities
├── temp/                  # Временные файлы
├── .env                   # Переменные окружения (заполнен)
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile             # Docker образ
├── main.py               # Entry point
└── requirements.txt      # Python зависимости
```

---

## Полезные команды

### Перезапуск конкретного сервиса
```bash
docker-compose restart web
docker-compose restart worker
```

### Просмотр логов конкретного сервиса
```bash
docker-compose logs -f web
docker-compose logs -f worker
```

### Выполнение команды в контейнере
```bash
docker exec -it ai-video-bot-web bash
docker exec -it ai-video-bot-worker bash
```

### Проверка FFmpeg
```bash
docker exec ai-video-bot-web ffmpeg -version
```

### Очистка и пересборка
```bash
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

---

## Документация

- 📄 **DEPLOYMENT_SUCCESS.md** - отчет о запуске
- 📄 **DOCKER_RUN.md** - подробная инструкция по Docker
- 📄 **README.md** - общая документация
- 📄 **QUICKSTART.md** - быстрый старт

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Перезапустите: `docker-compose restart`
4. Пересоберите: `docker-compose build && docker-compose up -d`

---

**Проект готов к работе! 🎉**
