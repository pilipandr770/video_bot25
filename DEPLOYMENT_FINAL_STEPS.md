# Финальные Шаги Деплоя на Render.com

## ✅ Что уже сделано

1. ✅ Код закоммичен и запушен в GitHub
2. ✅ Render.com аккаунт создан
3. ✅ Сервисы развернуты на Render
4. ✅ Environment variables настроены

## 🚀 Что нужно сделать сейчас

### Шаг 1: Проверить статус деплоя на Render

1. Откройте https://dashboard.render.com
2. Проверьте статус всех трех сервисов:
   - **ai-video-bot-web** - должен быть "Live" (зеленый)
   - **ai-video-bot-worker** - должен быть "Live" (зеленый)
   - **ai-video-redis** - должен быть "Available" (зеленый)

3. Если какой-то сервис в статусе "Build failed" или "Deploy failed":
   - Кликните на сервис
   - Откройте вкладку "Logs"
   - Найдите ошибку и исправьте

### Шаг 2: Получить URL вашего веб-сервиса

1. В Render dashboard кликните на **ai-video-bot-web**
2. Скопируйте URL (например: `https://ai-video-bot-web-xxxx.onrender.com`)
3. Сохраните этот URL - он понадобится для webhook

### Шаг 3: Проверить Health Check

Откройте в браузере или выполните в терминале:

```bash
curl https://your-app-name.onrender.com/health
```

Должен вернуться ответ:
```json
{
  "status": "healthy",
  "service": "ai-video-generator-bot"
}
```

### Шаг 4: Настроить Telegram Webhook

#### Вариант A: Автоматический (рекомендуется)

1. Откройте файл `setup_render_webhook_final.py`
2. Замените `RENDER_WEB_URL` на ваш реальный URL:
   ```python
   RENDER_WEB_URL = "https://ai-video-bot-web-xxxx.onrender.com"
   ```
3. Запустите скрипт:
   ```bash
   python setup_render_webhook_final.py
   ```

#### Вариант B: Вручную через curl

```bash
curl -X POST "https://api.telegram.org/bot8370296832:AAETV_lHGioGKllkVraWZE2JUOKoFF0gZas/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-app-name.onrender.com/webhook",
    "allowed_updates": ["message", "callback_query"]
  }'
```

### Шаг 5: Проверить Webhook

Проверьте статус webhook:

```bash
curl "https://api.telegram.org/bot8370296832:AAETV_lHGioGKllkVraWZE2JUOKoFF0gZas/getWebhookInfo"
```

Должен показать:
- `url`: ваш webhook URL
- `pending_update_count`: 0
- `last_error_date`: не должно быть

### Шаг 6: Протестировать бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Отправьте тестовое сообщение: "Создай рекламу для нового смартфона"

### Шаг 7: Мониторинг логов

Откройте логи в Render dashboard:

**Web Service:**
```
incoming_request method=POST path=/webhook
telegram_webhook_received
video_generation_task_started
```

**Worker Service:**
```
celery@worker ready
Task video_generation_task started
generating_script
```

## 🔍 Проверка работоспособности

### Checklist

- [ ] Все 3 сервиса в статусе "Live"/"Available"
- [ ] Health check возвращает 200 OK
- [ ] Webhook настроен (getWebhookInfo показывает правильный URL)
- [ ] Бот отвечает на /start
- [ ] Бот принимает текстовые сообщения
- [ ] Worker обрабатывает задачи (видно в логах)
- [ ] Redis подключен (нет ошибок подключения в логах)

## ⚠️ Возможные проблемы

### Проблема: Webhook не получает обновления

**Решение:**
1. Проверьте, что TELEGRAM_WEBHOOK_URL в Render environment variables правильный
2. Убедитесь, что webhook URL доступен публично
3. Проверьте логи веб-сервиса на наличие ошибок
4. Удалите и установите webhook заново

### Проблема: Worker не обрабатывает задачи

**Решение:**
1. Проверьте, что REDIS_URL одинаковый для web и worker
2. Убедитесь, что все API ключи настроены в worker environment variables
3. Проверьте логи worker на наличие ошибок подключения
4. Перезапустите worker сервис

### Проблема: Ошибки API (OpenAI, Runway)

**Решение:**
1. Проверьте, что API ключи правильные
2. Убедитесь, что у вас есть кредиты на аккаунтах
3. Проверьте лимиты API
4. Посмотрите детальные ошибки в логах worker

### Проблема: Нехватка места на диске

**Решение:**
1. Проверьте использование диска в Render dashboard
2. Увеличьте размер диска если нужно
3. Проверьте, что FILE_CLEANUP_HOURS работает (логи должны показывать cleanup)

## 📊 Мониторинг

### Метрики для отслеживания

1. **CPU Usage** - должен быть < 80%
2. **Memory Usage** - должен быть < 80%
3. **Disk Usage** - должен быть < 80%
4. **Response Time** - должен быть < 5 секунд для webhook
5. **Error Rate** - должен быть < 1%

### Настройка алертов

В Render dashboard:
1. Перейдите в Settings каждого сервиса
2. Настройте Notifications
3. Добавьте email для получения алертов

## 🎉 Готово!

После выполнения всех шагов ваш бот должен быть полностью функциональным на Render.com.

## 📝 Следующие шаги

1. Протестируйте все функции бота
2. Настройте мониторинг и алерты
3. Документируйте любые кастомные настройки
4. Планируйте масштабирование при необходимости

---

**Полезные ссылки:**
- Render Dashboard: https://dashboard.render.com
- Telegram Bot API: https://core.telegram.org/bots/api
- Логи сервисов: доступны в Render dashboard для каждого сервиса
