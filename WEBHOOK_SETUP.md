# Настройка Webhook для Telegram бота

## Проблема
Бот не реагирует на сообщения, потому что webhook настроен на `http://localhost:5000`, который недоступен для Telegram серверов.

## Решения

### ✅ Решение 1: Polling режим (для локального теста)

**Уже работает!** Запустите:
```bash
python test_bot_polling.py
```

Бот будет сам опрашивать Telegram каждые 10 секунд. Это простой способ для тестирования.

**Минусы:**
- Не использует основное приложение (Flask + Celery)
- Только для простого тестирования
- Не подходит для продакшена

---

### 🌐 Решение 2: ngrok (для локального теста с webhook)

#### Шаг 1: Установите ngrok

1. Скачайте: https://ngrok.com/download
2. Распакуйте в любую папку
3. Зарегистрируйтесь и получите authtoken: https://dashboard.ngrok.com/get-started/your-authtoken

#### Шаг 2: Запустите ngrok

```bash
ngrok http 5000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:5000
```

#### Шаг 3: Обновите .env

Скопируйте HTTPS URL из ngrok и обновите `.env`:
```env
TELEGRAM_WEBHOOK_URL=https://abc123.ngrok.io
```

**Важно:** Используйте HTTPS URL, не HTTP!

#### Шаг 4: Перезапустите контейнеры

```bash
docker-compose restart
```

#### Шаг 5: Проверьте логи

```bash
docker-compose logs web -f
```

Вы должны увидеть:
```
telegram_webhook_configured webhook_url=https://abc123.ngrok.io/webhook
```

#### Шаг 6: Тестируйте!

Отправьте `/start` боту в Telegram. Теперь он должен ответить!

**Минусы ngrok:**
- URL меняется при каждом перезапуске (бесплатная версия)
- Нужно каждый раз обновлять .env и перезапускать контейнеры

---

### 🚀 Решение 3: Деплой на Render.com (рекомендуется)

#### Шаг 1: Подготовьте репозиторий

Убедитесь, что все файлы в Git:
```bash
git add .
git commit -m "Ready for deployment"
git push
```

#### Шаг 2: Создайте аккаунт на Render.com

1. Зайдите на https://render.com
2. Зарегистрируйтесь через GitHub

#### Шаг 3: Создайте Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name:** ai-video-bot
   - **Environment:** Docker
   - **Region:** Frankfurt (ближе к России)
   - **Instance Type:** Free (для теста)

#### Шаг 4: Добавьте переменные окружения

В разделе "Environment" добавьте все переменные из `.env`:
```
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_WEBHOOK_URL=https://ai-video-bot.onrender.com
OPENAI_API_KEY=ваш_ключ
OPENAI_ASSISTANT_ID=ваш_id
RUNWAY_API_KEY=ваш_ключ
REDIS_URL=redis://redis:6379/0
LOG_LEVEL=INFO
```

**Важно:** `TELEGRAM_WEBHOOK_URL` должен быть вашим Render URL!

#### Шаг 5: Добавьте Redis

1. Нажмите "New +" → "Redis"
2. Создайте Redis instance
3. Скопируйте Internal Redis URL
4. Обновите `REDIS_URL` в переменных окружения Web Service

#### Шаг 6: Деплой

Нажмите "Create Web Service". Render автоматически:
- Соберет Docker образ
- Запустит контейнеры
- Настроит HTTPS

#### Шаг 7: Проверьте

Откройте `https://ваш-сервис.onrender.com/health`

Должны увидеть:
```json
{"service":"ai-video-generator-bot","status":"healthy"}
```

---

## Проверка текущего webhook

Проверьте, какой webhook установлен:

```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo
```

Или создайте скрипт:

```python
import os
from dotenv import load_dotenv
import requests

load_dotenv()
token = os.getenv('TELEGRAM_BOT_TOKEN')

response = requests.get(f'https://api.telegram.org/bot{token}/getWebhookInfo')
print(response.json())
```

---

## Удаление webhook (для переключения на polling)

```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/deleteWebhook
```

Или в Python:
```python
import os
from dotenv import load_dotenv
import requests

load_dotenv()
token = os.getenv('TELEGRAM_BOT_TOKEN')

response = requests.get(f'https://api.telegram.org/bot{token}/deleteWebhook')
print(response.json())
```

---

## Рекомендации

**Для локального теста:**
- Используйте `test_bot_polling.py` (самый простой способ)
- Или ngrok, если нужно тестировать webhook

**Для продакшена:**
- Деплой на Render.com, Heroku, или VPS
- Используйте webhook (быстрее и надежнее polling)
- Настройте мониторинг и логирование

---

## Troubleshooting

### Бот не отвечает
1. Проверьте логи: `docker-compose logs web -f`
2. Проверьте webhook: `getWebhookInfo`
3. Убедитесь, что URL доступен извне

### Ошибка "Webhook not found"
- Webhook не установлен или удален
- Проверьте `TELEGRAM_WEBHOOK_URL` в .env

### Ошибка "Connection refused"
- Telegram не может достучаться до вашего сервера
- Используйте ngrok или деплой

### ngrok URL не работает
- Убедитесь, что используете HTTPS, не HTTP
- Проверьте, что ngrok запущен
- Перезапустите контейнеры после изменения .env
