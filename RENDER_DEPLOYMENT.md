# 🚀 Деплой на Render.com

## ✅ Готовность к деплою

Проект полностью готов к деплою на Render.com:

- ✅ `render.yaml` настроен (Docker runtime, Frankfurt region)
- ✅ `Dockerfile` оптимизирован (FFmpeg включен)
- ✅ `docker-compose.yml` для локальной разработки
- ✅ `.gitignore` настроен (не коммитит .env)
- ✅ `requirements.txt` актуален
- ✅ Health check endpoint работает
- ✅ Все сервисы протестированы локально

---

## 📋 Что будет развернуто

### 1. Web Service (ai-video-bot-web)
- **Runtime:** Docker
- **Region:** Frankfurt (ближе к России/Европе)
- **Plan:** Starter ($7/месяц)
- **Port:** Автоматически от Render
- **Disk:** 10GB для временных файлов
- **Endpoints:**
  - `/` - главная страница
  - `/health` - health check
  - `/webhook` - Telegram webhook

### 2. Worker Service (ai-video-bot-worker)
- **Runtime:** Docker
- **Region:** Frankfurt
- **Plan:** Starter ($7/месяц)
- **Concurrency:** 2 workers
- **Disk:** 20GB для видео файлов
- **Tasks:**
  - Генерация видео через Runway
  - Обработка через OpenAI Assistant
  - Очистка старых файлов

### 3. Redis Service (ai-video-redis)
- **Plan:** Starter ($7/месяц)
- **Region:** Frankfurt
- **Memory:** 256MB
- **Policy:** allkeys-lru (автоочистка при заполнении)

**Итого:** ~$21/месяц (можно начать с Free tier для теста)

---

## 🔧 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

1. **Убедитесь, что все изменения закоммичены:**

```bash
git status
git add .
git commit -m "Ready for Render deployment"
```

2. **Запушьте в GitHub:**

```bash
git push origin main
```

Если репозиторий еще не создан:

```bash
# Создайте репозиторий на GitHub
# Затем:
git remote add origin https://github.com/ваш-username/video4min.git
git branch -M main
git push -u origin main
```

---

### Шаг 2: Создание аккаунта на Render

1. Зайдите на https://render.com
2. Нажмите "Get Started"
3. Зарегистрируйтесь через GitHub (рекомендуется)
4. Подтвердите email

---

### Шаг 3: Создание Blueprint (автоматический деплой)

1. В Render Dashboard нажмите **"New +"** → **"Blueprint"**

2. Подключите ваш GitHub репозиторий:
   - Выберите "Connect a repository"
   - Найдите `video4min` (или ваше название)
   - Нажмите "Connect"

3. Render автоматически обнаружит `render.yaml` и покажет:
   - ✅ ai-video-bot-web (Web Service)
   - ✅ ai-video-bot-worker (Background Worker)
   - ✅ ai-video-redis (Redis)

4. Нажмите **"Apply"**

---

### Шаг 4: Настройка переменных окружения

После создания Blueprint, настройте переменные для каждого сервиса:

#### Для Web Service (ai-video-bot-web):

Перейдите в настройки сервиса и добавьте:

```env
TELEGRAM_BOT_TOKEN=8370296832:AAETV_lHGioGKllkVraWZE2JUOKoFF0gZas
TELEGRAM_WEBHOOK_URL=https://ai-video-bot-web.onrender.com
OPENAI_API_KEY=sk-proj-...
OPENAI_ASSISTANT_ID=asst_...
RUNWAY_API_KEY=key_...
```

**Важно:** 
- `TELEGRAM_WEBHOOK_URL` должен быть вашим Render URL!
- После первого деплоя вы получите точный URL
- Обновите эту переменную после получения URL

#### Для Worker Service (ai-video-bot-worker):

```env
OPENAI_API_KEY=sk-proj-...
OPENAI_ASSISTANT_ID=asst_...
RUNWAY_API_KEY=key_...
```

**Примечание:** `REDIS_URL` настраивается автоматически через `render.yaml`

---

### Шаг 5: Деплой

1. Render автоматически начнет деплой после применения Blueprint

2. Следите за логами:
   - Откройте каждый сервис
   - Перейдите на вкладку "Logs"
   - Дождитесь сообщения "Deploy succeeded"

3. Процесс займет 5-10 минут:
   - Сборка Docker образа (~5 мин)
   - Установка зависимостей
   - Запуск сервисов

---

### Шаг 6: Получение URL и обновление webhook

1. После успешного деплоя откройте **ai-video-bot-web**

2. Скопируйте URL (например: `https://ai-video-bot-web.onrender.com`)

3. Обновите переменную окружения:
   - Перейдите в Environment
   - Найдите `TELEGRAM_WEBHOOK_URL`
   - Измените на ваш Render URL
   - Сохраните

4. Render автоматически перезапустит сервис

---

### Шаг 7: Проверка работы

#### 1. Проверьте Health Endpoint

```bash
curl https://ai-video-bot-web.onrender.com/health
```

Ожидаемый ответ:
```json
{"service":"ai-video-generator-bot","status":"healthy"}
```

#### 2. Проверьте Webhook в Telegram

```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo
```

Должно показать:
```json
{
  "ok": true,
  "result": {
    "url": "https://ai-video-bot-web.onrender.com/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

#### 3. Протестируйте бота

Отправьте `/start` вашему боту в Telegram. Он должен ответить!

---

## 📊 Мониторинг

### Просмотр логов

1. **Web Service:**
   - Dashboard → ai-video-bot-web → Logs
   - Смотрите incoming requests и webhook events

2. **Worker Service:**
   - Dashboard → ai-video-bot-worker → Logs
   - Смотрите Celery tasks и video generation

3. **Redis:**
   - Dashboard → ai-video-redis → Metrics
   - Смотрите memory usage и connections

### Метрики

Render предоставляет:
- CPU usage
- Memory usage
- Request count
- Response time
- Disk usage

---

## 🔄 Обновление приложения

### Автоматический деплой (рекомендуется)

1. Внесите изменения в код
2. Закоммитьте и запушьте:
```bash
git add .
git commit -m "Update feature"
git push
```
3. Render автоматически задеплоит изменения

### Ручной деплой

1. Dashboard → Сервис → Manual Deploy
2. Выберите ветку
3. Нажмите "Deploy"

---

## 💰 Оптимизация затрат

### Free Tier (для теста)

Render предоставляет 750 часов бесплатно в месяц:
- Можно запустить Web Service бесплатно
- Worker и Redis требуют платный план

### Starter Plan ($7/месяц за сервис)

Рекомендуется для продакшена:
- Web: $7/месяц
- Worker: $7/месяц
- Redis: $7/месяц
- **Итого: $21/месяц**

### Экономия

Если бюджет ограничен:
1. Используйте Free tier для Web
2. Worker на Starter ($7)
3. Redis на Starter ($7)
4. **Итого: $14/месяц**

Или используйте внешний Redis (Redis Labs, Upstash):
- Web: Free
- Worker: $7
- Redis: Free (Upstash)
- **Итого: $7/месяц**

---

## 🐛 Troubleshooting

### Деплой не удался

1. Проверьте логи сборки
2. Убедитесь, что `Dockerfile` корректен
3. Проверьте `requirements.txt`

### Бот не отвечает

1. Проверьте логи Web Service
2. Убедитесь, что `TELEGRAM_WEBHOOK_URL` правильный
3. Проверьте webhook: `getWebhookInfo`

### Worker не обрабатывает задачи

1. Проверьте логи Worker Service
2. Убедитесь, что Redis подключен
3. Проверьте переменные окружения

### Ошибка "Out of memory"

1. Увеличьте план (Standard вместо Starter)
2. Оптимизируйте код
3. Уменьшите concurrency в Worker

### Медленная генерация видео

1. Проверьте Runway API limits
2. Увеличьте timeout в Gunicorn
3. Оптимизируйте размер видео

---

## 📝 Чеклист перед деплоем

- ✅ Все изменения закоммичены и запушены
- ✅ `.env` не включен в Git (проверьте `.gitignore`)
- ✅ `render.yaml` настроен
- ✅ `Dockerfile` работает локально
- ✅ Все API ключи готовы
- ✅ GitHub репозиторий доступен
- ✅ Аккаунт на Render создан

---

## 🎯 После деплоя

1. **Обновите документацию:**
   - Добавьте Render URL в README
   - Обновите инструкции для пользователей

2. **Настройте мониторинг:**
   - Подключите Sentry для ошибок
   - Настройте алерты в Render

3. **Протестируйте все функции:**
   - Отправка текста
   - Генерация видео
   - Rate limiting
   - Обработка ошибок

4. **Оптимизируйте:**
   - Мониторьте использование ресурсов
   - Настройте кеширование
   - Оптимизируйте запросы к API

---

## 🚀 Готовы к деплою!

Все файлы настроены и готовы. Следуйте инструкции выше для деплоя на Render.com.

**Следующий шаг:** Создайте GitHub репозиторий и запушьте код, затем подключите к Render.
