# AI Video Generator Bot

Telegram бот для автоматической генерации 4-минутных рекламных видеороликов с использованием AI. Система принимает текстовое или голосовое описание темы и создает готовое видео с профессиональной озвучкой.

## Возможности

- 📝 Прием текстовых и голосовых сообщений
- 🤖 Автоматическая генерация сценария через OpenAI Assistant
- 🎨 Создание изображений через Runway API
- 🎬 Анимация изображений в видео через Runway API
- 🎙️ Профессиональная озвучка через OpenAI TTS
- ✅ Система утверждения на каждом этапе (сценарий, изображения, видео)
- 🎥 Автоматическая сборка финального видео через FFmpeg
- 📊 Отслеживание прогресса в реальном времени
- 🔄 Асинхронная обработка через Celery

## Архитектура

```
Telegram User → Telegram Bot API → Flask App → Celery Worker
                                                    ↓
                                    ┌───────────────┴───────────────┐
                                    ↓               ↓               ↓
                              OpenAI API      Runway API        FFmpeg
                           (Script, TTS)   (Images, Videos)   (Assembly)
```

## Требования

- Python 3.11+
- Redis (для Celery)
- FFmpeg (для обработки видео)
- Telegram Bot Token
- OpenAI API Key и Assistant ID
- Runway API Key

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd ai-video-generator-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Загрузка FFmpeg бинарников

FFmpeg необходим для сборки финального видео. Загрузите статическую сборку для вашей платформы:

#### Linux (для Render.com и production)

```bash
# Создайте директорию для FFmpeg
mkdir -p bin/ffmpeg

# Скачайте FFmpeg static build
wget https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz

# Распакуйте архив
tar -xf ffmpeg-release-amd64-static.tar.xz

# Скопируйте бинарники
cp ffmpeg-*-amd64-static/ffmpeg bin/ffmpeg/
cp ffmpeg-*-amd64-static/ffprobe bin/ffmpeg/

# Установите права на выполнение
chmod +x bin/ffmpeg/ffmpeg bin/ffmpeg/ffprobe

# Удалите временные файлы
rm -rf ffmpeg-*-amd64-static*
```

#### macOS

```bash
# Установите через Homebrew
brew install ffmpeg

# Или скопируйте бинарники в bin/ffmpeg/
mkdir -p bin/ffmpeg
cp $(which ffmpeg) bin/ffmpeg/
cp $(which ffprobe) bin/ffmpeg/
```

#### Windows

```bash
# Скачайте с https://www.gyan.dev/ffmpeg/builds/
# Распакуйте и скопируйте ffmpeg.exe и ffprobe.exe в bin/ffmpeg/
```

### 5. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_WEBHOOK_URL=https://your-app.onrender.com/webhook

# OpenAI
OPENAI_API_KEY=your_openai_api_key
OPENAI_ASSISTANT_ID=your_assistant_id

# Runway
RUNWAY_API_KEY=your_runway_api_key

# Redis (для локальной разработки)
REDIS_URL=redis://localhost:6379/0

# Application Settings (опционально)
TEMP_DIR=temp
MAX_CONCURRENT_JOBS=5
FILE_CLEANUP_HOURS=1
```

#### Получение API ключей

**Telegram Bot Token:**
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

**OpenAI API Key и Assistant ID:**
1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Создайте API ключ в разделе API Keys
3. Создайте Assistant в разделе Assistants с инструкциями для генерации сценариев
4. Скопируйте Assistant ID

**Runway API Key:**
1. Зарегистрируйтесь на [runwayml.com](https://runwayml.com)
2. Получите API ключ в настройках аккаунта

### 6. Запуск Redis (для локальной разработки)

```bash
# Через Docker
docker run -d -p 6379:6379 redis:alpine

# Или установите локально
# macOS: brew install redis && brew services start redis
# Ubuntu: sudo apt install redis-server && sudo systemctl start redis
```

## Локальный запуск

### Вариант 1: Docker (Рекомендуется)

Docker обеспечивает изолированную среду и упрощает развертывание.

#### Быстрый старт

```powershell
# Запустить все сервисы
docker-compose up -d --build

# Проверить статус
docker-compose ps

# Просмотреть логи
docker-compose logs -f
```

#### Комплексное тестирование

```powershell
# Windows PowerShell (рекомендуется)
.\test_docker_local.ps1

# Windows CMD
test_docker_local.cmd

# Быстрая проверка
.\docker_quick_test.ps1

# Мониторинг в реальном времени
.\docker_monitor.ps1
```

Подробная документация: [DOCKER_LOCAL_TESTING_GUIDE.md](./DOCKER_LOCAL_TESTING_GUIDE.md)

#### Управление контейнерами

```bash
# Остановить все сервисы
docker-compose down

# Перезапустить сервис
docker-compose restart web

# Просмотреть логи конкретного сервиса
docker-compose logs -f web
docker-compose logs -f worker
docker-compose logs -f redis

# Выполнить команду в контейнере
docker exec -it ai-video-bot-web bash
```

### Вариант 2: Локальная установка

#### 1. Запуск Flask приложения

```bash
python main.py
```

#### 2. Запуск Celery worker (в отдельном терминале)

```bash
celery -A app.tasks.celery worker --loglevel=info
```

#### 3. Настройка webhook для Telegram

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-ngrok-url.ngrok.io/webhook"}'
```

Для локальной разработки используйте [ngrok](https://ngrok.com/) для создания публичного URL:

```bash
ngrok http 5000
```

## Развертывание на Render.com

### 1. Подготовка репозитория

Убедитесь, что:
- FFmpeg бинарники находятся в `bin/ffmpeg/`
- Файл `render.yaml` настроен корректно
- Все зависимости указаны в `requirements.txt`

### 2. Создание сервисов на Render.com

1. Подключите GitHub репозиторий к Render.com
2. Render автоматически обнаружит `render.yaml` и создаст три сервиса:
   - **Web Service** (Flask приложение)
   - **Worker Service** (Celery worker)
   - **Redis Service**

### 3. Настройка переменных окружения

В настройках каждого сервиса добавьте:

```
TELEGRAM_BOT_TOKEN=<your_token>
OPENAI_API_KEY=<your_key>
OPENAI_ASSISTANT_ID=<your_assistant_id>
RUNWAY_API_KEY=<your_key>
```

`REDIS_URL` будет автоматически настроен через `render.yaml`.

### 4. Настройка Telegram webhook

После развертывания обновите webhook URL:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-app.onrender.com/webhook"}'
```

### 5. Мониторинг

- Логи доступны в панели Render.com для каждого сервиса
- Health check endpoint: `https://your-app.onrender.com/health`

## Использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start` для начала работы
3. Отправьте текстовое или голосовое описание темы рекламного ролика
4. Дождитесь генерации сценария и утвердите его
5. Просмотрите превью изображений и утвердите их
6. Просмотрите превью видео сегментов и утвердите их
7. Получите готовое 4-минутное видео с озвучкой

### Пример промпта

```
Создай рекламу для нового смартфона с акцентом на камеру и батарею. 
Покажи профессиональные фотографии, сделанные на телефон, и подчеркни 
длительное время работы без подзарядки.
```

## Структура проекта

```
ai-video-generator-bot/
├── app/
│   ├── bot/                    # Telegram bot handlers
│   │   ├── handlers.py
│   │   ├── webhook.py
│   │   └── notifications.py
│   ├── services/               # Business logic
│   │   ├── openai_service.py
│   │   ├── runway_service.py
│   │   ├── script_service.py
│   │   ├── video_service.py
│   │   ├── audio_service.py
│   │   └── approval_service.py
│   ├── tasks/                  # Celery tasks
│   │   └── video_generation.py
│   ├── utils/                  # Utilities
│   │   ├── ffmpeg.py
│   │   ├── file_manager.py
│   │   └── validators.py
│   ├── models/                 # Data models
│   │   └── video_job.py
│   └── config.py               # Configuration
├── bin/
│   └── ffmpeg/                 # FFmpeg binaries
├── temp/                       # Temporary files (gitignored)
├── .env                        # Environment variables (gitignored)
├── requirements.txt
├── Dockerfile
├── render.yaml
└── main.py                     # Application entry point
```

## Процесс генерации видео

1. **Получение запроса** (текст или голос)
2. **Генерация сценария** через OpenAI Assistant
3. **Утверждение сценария** пользователем
4. **Генерация изображений** (48 изображений через Runway API)
5. **Утверждение изображений** (превью первых 5)
6. **Анимация изображений** (48 видео по 5 секунд через Runway API)
7. **Утверждение видео** (превью первых 3)
8. **Генерация озвучки** через OpenAI TTS
9. **Сборка финального видео** через FFmpeg
10. **Отправка готового видео** пользователю

Общее время: ~15-30 минут в зависимости от загрузки API

## Ограничения

- Максимум 5 одновременных задач генерации
- Rate limit: 5 запросов в минуту, 20 в час на пользователя
- Максимальный размер голосового сообщения: 20 МБ
- Максимальный размер финального видео: 50 МБ (лимит Telegram)
- Timeout на утверждение: 10 минут

## Обработка ошибок

Система автоматически обрабатывает следующие ошибки:

- **OpenAI rate limits**: Retry с экспоненциальной задержкой (3 попытки)
- **Runway timeouts**: Retry с линейной задержкой (2 попытки)
- **FFmpeg errors**: Автоматическое сжатие видео при превышении размера
- **Network errors**: Автоматические повторные попытки

Пользователь получает понятные сообщения об ошибках без технических деталей.

## Очистка временных файлов

Система автоматически удаляет временные файлы:
- После успешной отправки видео
- При отмене задачи пользователем
- Через 1 час после создания (scheduled task)

## Разработка

### Запуск тестов

```bash
pytest
```

### Форматирование кода

```bash
black app/
isort app/
```

### Проверка типов

```bash
mypy app/
```

## Troubleshooting

### FFmpeg не найден

```bash
# Проверьте наличие бинарников
ls -la bin/ffmpeg/

# Проверьте права на выполнение
chmod +x bin/ffmpeg/ffmpeg bin/ffmpeg/ffprobe

# Проверьте путь в config.py
echo $FFMPEG_PATH
```

### Redis connection error

```bash
# Проверьте, что Redis запущен
redis-cli ping

# Проверьте REDIS_URL в .env
echo $REDIS_URL
```

### Webhook не работает

```bash
# Проверьте текущий webhook
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"

# Удалите webhook
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"

# Установите заново
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-app.onrender.com/webhook"}'
```

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создайте issue в GitHub репозитории.
