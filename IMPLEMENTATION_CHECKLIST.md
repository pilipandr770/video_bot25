# ✅ Чеклист внедрения Enhanced Pipeline

## 📦 Что создано

### Новые файлы:

- ✅ `app/models/video_job_enhanced.py` — модели БД с детальным трекингом
- ✅ `app/services/openai_service.py` — обновлён для работы с 3 ассистентами
- ✅ `app/tasks/video_generation_enhanced.py` — новый пайплайн с пошаговой генерацией
- ✅ `app/bot/handlers_enhanced.py` — обработчики с постоянными кнопками
- ✅ `migrations/create_enhanced_tables.sql` — SQL миграция для новых таблиц
- ✅ `apply_enhanced_migration.py` — скрипт применения миграции
- ✅ `ASSISTANT_INSTRUCTIONS.md` — инструкции для 3 ассистентов (готовы к копипасту)
- ✅ `ENHANCED_PIPELINE_GUIDE.md` — полное руководство
- ✅ `DEPLOY_ENHANCED.md` — быстрая инструкция по деплою
- ✅ `IMPLEMENTATION_CHECKLIST.md` — этот файл

### Обновлённые файлы:

- ✅ `.env` — добавлены ID трёх ассистентов

---

## 🎯 Что нужно сделать

### Этап 1: Подготовка (5 минут)

- [ ] **1.1** Убедиться, что все 3 ассистента созданы в OpenAI Playground
  - Script Assistant: `asst_1Yu1uxDttuH0t3Oux7V09BZt`
  - Segment Assistant: `asst_bKhMND0deCZKS6IyPMbm6uW6`
  - Animation Assistant: `asst_HWZcPW86OtN7YgC1frNLhKuo`

- [ ] **1.2** Проверить, что ID добавлены в `.env` локально
  ```bash
  cat .env | grep OPENAI_.*_ASSISTANT_ID
  ```

- [ ] **1.3** Проверить, что ID добавлены на Render
  - Web Service → Environment
  - Worker Service → Environment

### Этап 2: База данных (5 минут)

- [ ] **2.1** Применить миграцию локально
  ```bash
  python apply_enhanced_migration.py
  ```

- [ ] **2.2** Проверить, что таблицы созданы
  ```bash
  psql $DATABASE_URL -c "\dt ai_video_bot.*enhanced*"
  ```

- [ ] **2.3** Применить миграцию на Render
  - Вариант A: через Shell в Render Dashboard
  - Вариант B: через psql с External Database URL

### Этап 3: Обновление кода (10 минут)

- [ ] **3.1** Обновить `app/bot/webhook.py`
  
  Заменить импорты:
  ```python
  # Было:
  from app.bot.handlers import handle_start, handle_message, handle_voice, handle_callback_query
  
  # Стало:
  from app.bot.handlers_enhanced import (
      handle_start, 
      handle_message, 
      handle_status, 
      handle_approve, 
      handle_reject
  )
  ```

- [ ] **3.2** Обновить регистрацию обработчиков в `app/bot/webhook.py`
  
  Добавить:
  ```python
  application.add_handler(CommandHandler("status", handle_status))
  # MessageHandler уже должен быть, но убедитесь что он использует handle_message из handlers_enhanced
  ```

- [ ] **3.3** Проверить импорты в `app/__init__.py` или `run_web.py`
  
  Убедиться, что используется правильный модуль обработчиков

### Этап 4: Локальное тестирование (15 минут)

- [ ] **4.1** Запустить web service
  ```bash
  python run_web.py
  ```

- [ ] **4.2** Запустить worker service
  ```bash
  python run_worker.py
  ```

- [ ] **4.3** Отправить тестовое сообщение боту
  - Текст: "Реклама кофе"

- [ ] **4.4** Проверить, что появились кнопки
  - 📊 Статус
  - 🔄 Старт
  - ✅ Подтвердить
  - ❌ Отклонить

- [ ] **4.5** Нажать "Статус" и проверить отображение прогресса

- [ ] **4.6** Дождаться сценария и нажать "Подтвердить"

- [ ] **4.7** Следить за прогрессом через "Статус"
  - Промпты для изображений: 0/10 → 10/10
  - Изображения: 0/10 → 10/10
  - Промпты для анимации: 0/10 → 10/10
  - Видео: 0/10 → 10/10

- [ ] **4.8** Проверить логи на наличие ошибок
  ```bash
  tail -f logs/app.log
  ```

### Этап 5: Деплой на Render (10 минут)

- [ ] **5.1** Закоммитить все изменения
  ```bash
  git add .
  git commit -m "feat: Enhanced pipeline with 3 assistants and detailed tracking"
  ```

- [ ] **5.2** Запушить на GitHub
  ```bash
  git push origin main
  ```

- [ ] **5.3** Дождаться автоматического деплоя на Render
  - Web Service → Logs (следить за процессом)
  - Worker Service → Logs (следить за процессом)

- [ ] **5.4** Проверить, что сервисы запустились без ошибок
  - Web Service → Status: Live (зелёный)
  - Worker Service → Status: Live (зелёный)

- [ ] **5.5** Проверить health endpoint
  ```bash
  curl https://video-bot25.onrender.com/health
  ```

### Этап 6: Тестирование в продакшене (20 минут)

- [ ] **6.1** Отправить тестовое сообщение боту в Telegram

- [ ] **6.2** Проверить кнопки

- [ ] **6.3** Проверить команду /status

- [ ] **6.4** Дождаться сценария и утвердить

- [ ] **6.5** Следить за прогрессом через кнопку "Статус"

- [ ] **6.6** Дождаться завершения генерации

- [ ] **6.7** Проверить финальное видео

- [ ] **6.8** Проверить логи на Render
  - Web Service → Logs
  - Worker Service → Logs

### Этап 7: Мониторинг (опционально)

- [ ] **7.1** Проверить данные в БД
  ```sql
  SELECT id, status, created_at, completed_at 
  FROM ai_video_bot.video_jobs_enhanced 
  ORDER BY created_at DESC 
  LIMIT 5;
  ```

- [ ] **7.2** Проверить прогресс по сегментам
  ```sql
  SELECT 
      segment_index,
      CASE WHEN image_prompt IS NOT NULL THEN '✅' ELSE '⏳' END as img_prompt,
      CASE WHEN image_path IS NOT NULL THEN '✅' ELSE '⏳' END as image,
      CASE WHEN animation_prompt IS NOT NULL THEN '✅' ELSE '⏳' END as anim_prompt,
      CASE WHEN video_path IS NOT NULL THEN '✅' ELSE '⏳' END as video
  FROM ai_video_bot.video_segments_enhanced
  WHERE job_id = 'YOUR_JOB_ID'
  ORDER BY segment_index;
  ```

---

## 🐛 Troubleshooting

### Проблема: "Assistant ID is required"

**Решение:**
```bash
# Проверить переменные
echo $OPENAI_SCRIPT_ASSISTANT_ID
echo $OPENAI_SEGMENT_ASSISTANT_ID
echo $OPENAI_ANIMATION_ASSISTANT_ID

# Если пусто, добавить в .env и перезапустить
```

### Проблема: Таблицы не создаются

**Решение:**
```bash
# Проверить подключение к БД
psql $DATABASE_URL -c "SELECT version();"

# Проверить схему
psql $DATABASE_URL -c "SELECT schema_name FROM information_schema.schemata WHERE schema_name = 'ai_video_bot';"

# Применить миграцию вручную
psql $DATABASE_URL < migrations/create_enhanced_tables.sql
```

### Проблема: Кнопки не появляются

**Решение:**
- Убедиться, что используется `handlers_enhanced.py`
- Проверить, что `MAIN_KEYBOARD` передаётся в `reply_markup`
- Перезапустить бота (отправить /start)

### Проблема: Worker не обрабатывает задачи

**Решение:**
```bash
# Проверить логи worker
tail -f logs/celery.log

# Проверить подключение к Redis/PostgreSQL
# Проверить, что задача зарегистрирована
celery -A app.tasks inspect registered
```

---

## 📊 Метрики успеха

После внедрения проверьте:

- ✅ Время генерации: ~15-20 минут (было ~15-20 минут)
- ✅ Качество промптов: улучшено за счёт специализированных ассистентов
- ✅ Прозрачность процесса: пользователь видит детальный прогресс
- ✅ Удобство управления: постоянные кнопки вместо inline
- ✅ Надёжность: все данные сохраняются в БД

---

## 🎉 Готово!

Когда все пункты отмечены, у вас:

- ✅ Работает enhanced pipeline с 3 ассистентами
- ✅ Детальное отслеживание в БД
- ✅ Постоянные кнопки управления
- ✅ Пошаговая генерация с сохранением прогресса
- ✅ Возможность отслеживать статус в реальном времени

**Следующие шаги:**
1. Мониторить первые несколько генераций
2. Собрать feedback от пользователей
3. Оптимизировать промпты ассистентов при необходимости
4. Добавить аналитику и метрики

---

**Дата:** 14 ноября 2025  
**Версия:** 2.0 Enhanced Pipeline  
**Статус:** Готово к внедрению ✅
