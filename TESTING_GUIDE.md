# 🧪 Руководство по тестированию бота

## Шаг 1: Проверка базы данных (локально)

Перед тестированием бота убедитесь, что база данных работает:

```bash
python test_db.py
```

Ожидаемый результат:
```
✅ Database initialized successfully!
✅ Session created successfully!
✅ VideoJob created successfully!
✅ All database tests passed!
```

---

## Шаг 2: Тестирование бота в Telegram

### 2.1 Команда /start

**Действие:** Отправьте `/start` боту @Video4min_bot

**Ожидаемый результат:**
```
👋 Привет, [Ваше имя]!

Я бот для автоматической генерации рекламных видеороликов.

📝 Отправьте мне текстовое или голосовое описание вашего рекламного ролика, 
и я создам для вас профессиональное 50-секундное видео с озвучкой.

...
```

---

### 2.2 Отправка текстового запроса

**Действие:** Отправьте текст, например:
```
Реклама кофе для утра
```

**Ожидаемый результат:**
```
✅ Ваш запрос принят!

🆔 ID задачи: `[UUID]`

⏱️ Примерное время генерации: 15-20 минут

Я буду отправлять вам обновления на каждом этапе...

Начинаю работу... 🚀
```

Затем через несколько секунд:
```
📝 Генерирую сценарий...
```

---

### 2.3 Получение сценария

**Ожидаемый результат через 10-30 секунд:**

Бот отправит сценарий с кнопками:
```
📝 Сценарий готов!

[Текст сценария на 50 секунд]

Одобрить этот сценарий?

[✅ Одобрить] [❌ Отклонить]
```

**Действие:** Нажмите "✅ Одобрить"

---

### 2.4 Генерация изображений

**Ожидаемый результат:**
```
🎨 Генерирую изображения (0/10)...
🎨 Генерирую изображения (3/10)...
🎨 Генерирую изображения (6/10)...
🎨 Генерирую изображения (9/10)...
```

Затем бот отправит первые 5 изображений для предпросмотра с кнопками одобрения.

---

## Шаг 3: Проверка логов на Render

Если что-то не работает, проверьте логи:

1. Откройте https://dashboard.render.com
2. Выберите сервис `video-bot25`
3. Перейдите в раздел "Logs"
4. Ищите сообщения с вашим `job_id`

### Ключевые логи для поиска:

```
video_generation_started job_id=...
video_job_created_in_db job_id=...
stage_started stage=generate_script
stage_completed stage=generate_script
```

---

## Шаг 4: Проверка базы данных на Render

Если нужно проверить, что записи создаются в БД:

1. Откройте Render Dashboard
2. Перейдите в PostgreSQL сервис
3. Подключитесь через psql или используйте Shell:

```sql
-- Подключиться к БД
\c ittoken_db

-- Установить схему
SET search_path TO ai_video_bot;

-- Проверить таблицы
\dt

-- Посмотреть задания
SELECT id, user_id, status, created_at FROM video_jobs ORDER BY created_at DESC LIMIT 5;

-- Посмотреть сегменты
SELECT job_id, segment_index, section, image_status, video_status FROM video_segments LIMIT 10;

-- Посмотреть одобрения
SELECT job_id, approval_type, status FROM approvals ORDER BY created_at DESC LIMIT 5;
```

---

## Возможные проблемы и решения

### Проблема 1: Бот не отвечает на /start

**Проверка:**
- Webhook настроен? `curl https://api.telegram.org/bot[TOKEN]/getWebhookInfo`
- Сервис запущен? Проверьте статус на Render

**Решение:**
- Перезапустите сервис на Render
- Проверьте логи на наличие ошибок

---

### Проблема 2: "Произошла ошибка при запуске задачи"

**Проверка:**
- Celery работает? (Пока нет worker'а, задачи не выполняются)
- База данных доступна?

**Решение:**
- Проверьте логи: `failed_to_start_video_generation_task`
- Убедитесь, что `DATABASE_URL` настроен правильно

---

### Проблема 3: Задача запустилась, но сценарий не генерируется

**Проверка:**
- `OPENAI_API_KEY` настроен?
- `OPENAI_SCRIPT_ASSISTANT_ID` существует?

**Решение:**
- Проверьте логи: `stage_started stage=generate_script`
- Убедитесь, что Assistant создан в OpenAI Playground

---

### Проблема 4: Schema errors в логах

**Проверка:**
```
no schema has been selected to create in
```

**Решение:**
- Убедитесь, что `DATABASE_SCHEMA=ai_video_bot` в env переменных
- Проверьте, что схема создана в БД

---

## Текущий статус реализации

✅ **Реализовано:**
- Webhook и обработка команд
- База данных с правильной схемой
- Создание VideoJob при запуске задачи
- Структура для генерации сценария

⏳ **В процессе:**
- Celery worker (нужно запустить отдельный сервис)
- OpenAI Assistant интеграция
- Runway API интеграция

❌ **Не реализовано:**
- Celery worker service на Render
- Полный пайплайн генерации видео

---

## Следующие шаги

1. ✅ Протестировать `/start` команду
2. ✅ Протестировать отправку текста
3. ⏳ Запустить Celery worker на Render
4. ⏳ Протестировать генерацию сценария
5. ⏳ Протестировать систему одобрений

---

**Дата обновления:** 13 ноября 2025
