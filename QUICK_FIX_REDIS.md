# Быстрое исправление Redis на Render

## Проблема
Бот не может подключиться к Redis: `Connection refused to localhost:6379`

## Решение (2 минуты)

### 1. Откройте Render Dashboard
https://dashboard.render.com

### 2. Исправьте Web Service

1. Кликните на **ai-video-bot-web**
2. Перейдите в **Environment** (левое меню)
3. Найдите `REDIS_URL`
4. Если значение `redis://localhost:6379/0` - это неправильно!
5. Измените на:
   - Нажмите "Edit"
   - Выберите "Sync from Service"
   - Service: `ai-video-redis`
   - Property: `connectionString`
6. Нажмите "Save Changes"

### 3. Исправьте Worker Service

1. Кликните на **ai-video-bot-worker**
2. Повторите те же шаги, что и для web service

### 4. Подождите перезапуска

- Render автоматически перезапустит оба сервиса
- Подождите ~2-3 минуты
- Статус должен стать "Live" (зеленый)

### 5. Проверьте

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Отправьте любое сообщение
4. Проверьте логи - ошибок Redis быть не должно

## Правильная конфигурация REDIS_URL

**Неправильно:**
```
redis://localhost:6379/0
```

**Правильно (Internal URL):**
```
redis://red-xxxxxxxxxxxxx:6379
```

**Или через Sync from Service:**
- Type: Redis
- Service: ai-video-redis
- Property: connectionString

---

После исправления бот должен заработать! 🎉
