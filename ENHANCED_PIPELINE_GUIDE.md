# 🚀 Enhanced Pipeline - Руководство по обновлению

## Что нового?

### ✨ Основные улучшения:

1. **Три специализированных OpenAI ассистента:**
   - Script Assistant — генерация сценариев
   - Segment Assistant — создание промптов для изображений
   - Animation Assistant — создание промптов для анимации

2. **Детальная таблица в БД:**
   - Хранение всех промежуточных данных
   - Отслеживание каждого шага генерации
   - 10 сегментов по 5 секунд = 50-секундное видео

3. **Постоянные кнопки в боте:**
   - 📊 Статус — показывает текущий прогресс
   - ✅ Подтвердить — утверждает текущий этап
   - ❌ Отклонить — отменяет задачу и очищает БД
   - 🔄 Старт — показывает приветствие

4. **Пошаговая генерация:**
   - Каждый этап выполняется последовательно
   - Данные сохраняются в БД после каждого шага
   - Прогресс виден в реальном времени

---

## 📋 Этапы генерации

### Полный пайплайн (13 этапов):

1. **Пользователь отправляет тему** → Создаётся запись в БД
2. **Генерация сценария** (Script Assistant) → Сохраняется в `video_jobs_enhanced.script_text`
3. **Утверждение сценария** → Пользователь нажимает "Подтвердить"
4. **Разбивка на 10 сегментов** → Создаются записи в `video_segments_enhanced`
5. **Генерация 10 промптов для изображений** (Segment Assistant) → Заполняется `image_prompt`
6. **Генерация 10 изображений** (Runway API) → Заполняется `image_path`
7. **Утверждение изображений** → Показываются первые 5, пользователь подтверждает
8. **Генерация 10 промптов для анимации** (Animation Assistant) → Заполняется `animation_prompt`
9. **Анимация 10 видео** (Runway API) → Заполняется `video_path`
10. **Утверждение видео** → Показываются первые 3, пользователь подтверждает
11. **Генерация озвучки** (OpenAI TTS) → Сохраняется в `audio_path`
12. **Сборка финального видео** (FFmpeg) → Склейка всех частей
13. **Отправка готового ролика** → Пользователь получает видео

---

## 🗄️ Структура базы данных

### Таблица `video_jobs_enhanced`

Хранит основную информацию о задаче:

```sql
- id (VARCHAR) — уникальный ID задачи
- user_id (INTEGER) — Telegram user ID
- chat_id (INTEGER) — Telegram chat ID
- prompt (TEXT) — описание от пользователя
- status (VARCHAR) — текущий статус
- script_text (TEXT) — сгенерированный сценарий
- script_approved (INTEGER) — 0=ожидание, 1=утверждён, -1=отклонён
- audio_path (VARCHAR) — путь к озвучке
- final_video_path (VARCHAR) — путь к финальному видео
- created_at, updated_at, completed_at (TIMESTAMP)
```

### Таблица `video_segments_enhanced`

Хранит данные по каждому из 10 сегментов:

```sql
- id (SERIAL) — автоинкремент
- job_id (VARCHAR) — связь с video_jobs_enhanced
- segment_index (INTEGER) — номер сегмента (0-9)
- text (TEXT) — текст сегмента из сценария
- start_time, end_time (FLOAT) — тайминг (0-5, 5-10, и т.д.)

# Этап изображений:
- image_prompt (TEXT) — промпт от Segment Assistant
- image_prompt_generated_at (TIMESTAMP)
- image_path (VARCHAR) — путь к изображению
- image_runway_task_id (VARCHAR) — ID задачи в Runway
- image_generated_at (TIMESTAMP)

# Этап анимации:
- animation_prompt (TEXT) — промпт от Animation Assistant
- animation_prompt_generated_at (TIMESTAMP)
- video_path (VARCHAR) — путь к видео
- video_runway_task_id (VARCHAR) — ID задачи в Runway
- video_duration (FLOAT) — длительность видео
- video_generated_at (TIMESTAMP)

- status (VARCHAR) — pending, image_prompt_ready, image_ready, animation_prompt_ready, video_ready
```

---

## 🔧 Установка и настройка

### Шаг 1: Применить миграцию БД

```bash
# Локально
python apply_enhanced_migration.py

# На Render (через Shell)
python apply_enhanced_migration.py
```

### Шаг 2: Обновить переменные окружения

Убедитесь, что в `.env` (локально) и на Render есть все три ассистента:

```env
OPENAI_SCRIPT_ASSISTANT_ID=asst_xxx
OPENAI_SEGMENT_ASSISTANT_ID=asst_yyy
OPENAI_ANIMATION_ASSISTANT_ID=asst_zzz
```

### Шаг 3: Обновить код бота

Замените импорты в `app/bot/webhook.py`:

```python
# Старый импорт
from app.bot.handlers import handle_start, handle_message, handle_voice, handle_callback_query

# Новый импорт
from app.bot.handlers_enhanced import handle_start, handle_message, handle_status, handle_approve, handle_reject
```

### Шаг 4: Обновить регистрацию обработчиков

В `app/bot/webhook.py` или `run_bot.py`:

```python
# Добавить обработчики для кнопок
application.add_handler(CommandHandler("start", handle_start))
application.add_handler(CommandHandler("status", handle_status))
application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
```

### Шаг 5: Перезапустить сервисы

```bash
# Локально
# Остановить текущие процессы (Ctrl+C)
# Запустить заново:
python run_web.py
python run_worker.py

# На Render
# Сервисы перезапустятся автоматически после git push
```

---

## 🎮 Использование бота

### Команды и кнопки:

1. **🔄 Старт** (`/start`) — показывает приветствие и инструкции
2. **📊 Статус** (`/status`) — показывает детальный прогресс текущей задачи
3. **✅ Подтвердить** — утверждает текущий этап (сценарий/изображения/видео)
4. **❌ Отклонить** — отменяет задачу и очищает БД

### Пример использования:

```
Пользователь: "Реклама кофе"
Бот: ✅ Запрос принят! ID: abc-123...

[Через 30 секунд]
Бот: 📝 Сценарий готов! [текст сценария]
     ✅ Подтвердить | ❌ Отклонить

Пользователь: [нажимает ✅ Подтвердить]
Бот: ✅ Сценарий утверждён! Начинаю генерацию...

[Пользователь нажимает 📊 Статус]
Бот: 
📊 Статус задачи
🆔 ID: abc-123
📊 Статус: Обработка
✅ Сценарий: утверждён
📊 Сегменты (10 шт.):
• Промпты для изображений: 7/10
• Изображения: 3/10
• Промпты для анимации: 0/10
• Видео: 0/10
```

---

## 📊 Мониторинг прогресса

### Через кнопку "Статус":

Показывает:
- ID задачи
- Текущий статус
- Прогресс по сценарию
- Прогресс по сегментам (промпты, изображения, анимация, видео)
- Статус озвучки
- Информацию о финальном видео
- Временные метки

### Через логи:

```bash
# Локально
tail -f logs/app.log

# На Render
# Dashboard → Service → Logs
```

### Через БД:

```sql
-- Посмотреть все задачи
SELECT id, status, created_at, completed_at 
FROM ai_video_bot.video_jobs_enhanced 
ORDER BY created_at DESC 
LIMIT 10;

-- Посмотреть прогресс по сегментам
SELECT 
    segment_index,
    CASE WHEN image_prompt IS NOT NULL THEN '✅' ELSE '⏳' END as img_prompt,
    CASE WHEN image_path IS NOT NULL THEN '✅' ELSE '⏳' END as image,
    CASE WHEN animation_prompt IS NOT NULL THEN '✅' ELSE '⏳' END as anim_prompt,
    CASE WHEN video_path IS NOT NULL THEN '✅' ELSE '⏳' END as video,
    status
FROM ai_video_bot.video_segments_enhanced
WHERE job_id = 'abc-123'
ORDER BY segment_index;
```

---

## 🔄 Миграция со старой версии

Если у вас уже есть работающий бот:

1. **Создайте новые таблицы** (не удаляя старые):
   ```bash
   python apply_enhanced_migration.py
   ```

2. **Обновите код** постепенно:
   - Сначала добавьте новые обработчики
   - Протестируйте локально
   - Задеплойте на Render

3. **Старые таблицы** (`video_jobs`, `approval_requests`) можно оставить для истории

4. **Переключитесь** на новый пайплайн:
   - Измените импорты в `webhook.py`
   - Используйте `generate_video_enhanced_task` вместо `generate_video_task`

---

## ⚠️ Важные замечания

### Стоимость:

- **Script Assistant**: ~1 вызов на задачу
- **Segment Assistant**: 10 вызовов на задачу (по одному на сегмент)
- **Animation Assistant**: 10 вызовов на задачу (по одному на сегмент)
- **Итого**: 21 вызов OpenAI API на одно видео

### Время генерации:

- Сценарий: ~10-20 секунд
- Промпты для изображений: ~30-60 секунд (10 вызовов)
- Изображения: ~2-5 минут (Runway API)
- Промпты для анимации: ~30-60 секунд (10 вызовов)
- Анимация видео: ~5-10 минут (Runway API)
- Озвучка и сборка: ~30-60 секунд
- **Итого**: ~15-20 минут

### Ограничения:

- Максимум 10 сегментов (50 секунд видео)
- Размер финального видео: до 50 МБ (Telegram лимит)
- Timeout для утверждения: 10 минут

---

## 🐛 Troubleshooting

### Ошибка "Assistant ID is required"

Проверьте, что все три ассистента настроены:
```bash
echo $OPENAI_SCRIPT_ASSISTANT_ID
echo $OPENAI_SEGMENT_ASSISTANT_ID
echo $OPENAI_ANIMATION_ASSISTANT_ID
```

### Таблицы не создаются

Проверьте права доступа к БД и схему:
```sql
SELECT schema_name FROM information_schema.schemata WHERE schema_name = 'ai_video_bot';
```

### Кнопки не появляются

Убедитесь, что используете `handlers_enhanced.py` и `MAIN_KEYBOARD` передаётся в `reply_markup`.

---

## 📝 Следующие шаги

1. ✅ Применить миграцию БД
2. ✅ Обновить переменные окружения
3. ✅ Обновить код бота
4. ✅ Протестировать локально
5. ✅ Задеплоить на Render
6. ✅ Протестировать в продакшене

---

**Дата создания:** 14 ноября 2025
**Версия:** 2.0 (Enhanced Pipeline)
